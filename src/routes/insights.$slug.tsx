import { Link, createFileRoute, notFound } from "@tanstack/react-router";
import { ArrowLeft } from "lucide-react";
import { ContactBlock, pageWrap } from "@/components/portfolio";
import { insights } from "@/lib/portfolio-data";
export const Route=createFileRoute("/insights/$slug")({loader:({params})=>{const insight=insights.find(i=>i.slug===params.slug);if(!insight)throw notFound();return insight},head:({loaderData})=>({meta:[{title:loaderData?`${loaderData.title} — Ahmed Ali`:"Insight Not Found — Ahmed Ali"},{name:"description",content:loaderData?.excerpt??"The requested article could not be found."},{property:"og:title",content:loaderData?.title??"Insight Not Found"},{property:"og:description",content:loaderData?.excerpt??"Article unavailable."},{property:"og:type",content:"article"},{name:"twitter:card",content:"summary_large_image"}]}),component:Article});
function Article(){const item=Route.useLoaderData();return <><article className="bg-paper pt-16"><header className={`${pageWrap} py-16 sm:py-24`}><Link to="/insights" className="inline-flex items-center gap-2 font-mono text-[10px] uppercase tracking-[0.14em] text-ink-soft"><ArrowLeft className="size-4"/>All insights</Link><p className="eyebrow mt-12">{item.category} · {item.date}</p><h1 className="mt-5 max-w-[20ch] font-serif text-4xl leading-[1.06] sm:text-6xl">{item.title}</h1><p className="mt-6 max-w-[58ch] text-lg leading-8 text-ink-soft">{item.excerpt}</p><p className="mt-6 font-mono text-[10px] uppercase tracking-[0.14em] text-ink-soft">{item.time}</p></header><div className="border-t border-line"><div className={`${pageWrap} py-16`}><div className="mx-auto max-w-[700px] space-y-8 font-serif text-xl leading-9 text-ink-soft">{item.paragraphs.map(p=><p key={p}>{p}</p>)}</div></div></div></article><ContactBlock includeForm={false}/></>}